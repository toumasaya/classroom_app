<div class="col-md-12">
  <div class="course">
    <%= link_to '編輯', edit_course_path(@course), class: "btn btn-xs pull-right" %>

    <h2><%= @course.title %></h2>
    <p><%= @course.description %></p>
  </div>
</div>
